<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DungeonApp.ViewModels"
             xmlns:sessions="clr-namespace:DungeonApp.Views.Sessions"
             x:Class="DungeonApp.Views.SessionDetailView"
             x:DataType="vm:SessionDetailViewModel"
             x:Name="Root">

    <Grid RowDefinitions="Auto, Auto, *" Margin="30">
        
        <!-- NAGŁÓWEK WIDOKU SESJI -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" Margin="0,0,0,24">
            <!-- Przycisk powrotu -->
            <Button Grid.Column="0" Command="{Binding BackCommand}" 
                    Background="Transparent" BorderThickness="0" Padding="8,4" Margin="0,0,20,0"
                    Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center">
                <Button.Styles>
                    <Style Selector="Button:pointerover /template/ ContentPresenter">
                        <Setter Property="Background" Value="{DynamicResource BorderSecondary}"/>
                    </Style>
                </Button.Styles>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="←" FontSize="16" />
                    <TextBlock Text="Wróć do kampanii" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <!-- Tytuł sesji -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{Binding Campaign.Name}" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,12,0"/>
                <TextBlock Text="/" FontSize="26" Foreground="{DynamicResource BorderSecondary}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding Session.Title}" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
            </StackPanel>
        </Grid>

        <!-- NAVBAR DLA PRZEŁĄCZANYCH ZAKŁADEK -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,24">
            <!-- Przełącznik do Notatek -->
            <ToggleButton x:Name="NavNotesToggle" Click="NavToggle_Click"
                          Background="{DynamicResource InputBackground}" BorderBrush="{DynamicResource BorderInput}" BorderThickness="1" CornerRadius="8" Padding="16,8">
                <ToggleButton.Styles>
                    <Style Selector="ToggleButton:pointerover /template/ ContentPresenter">
                        <Setter Property="Background" Value="{DynamicResource HoverGrip}"/>
                    </Style>
                    <Style Selector="ToggleButton:checked /template/ ContentPresenter">
                        <Setter Property="Background" Value="{DynamicResource AccentPrimaryDark}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimary}"/>
                    </Style>
                </ToggleButton.Styles>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <PathIcon Data="{StaticResource IconEdit}" Width="14" Height="14" Foreground="{DynamicResource AccentPrimary}"/>
                    <TextBlock Text="Notatki Sesji" FontSize="13" Foreground="{DynamicResource TextSecondary}" FontWeight="SemiBold"/>
                </StackPanel>
            </ToggleButton>

            <!-- Przełącznik do Walki -->
            <ToggleButton x:Name="NavCombatToggle" Click="NavToggle_Click"
                          Background="{DynamicResource InputBackground}" BorderBrush="{DynamicResource BorderInput}" BorderThickness="1" CornerRadius="8" Padding="16,8">
                <ToggleButton.Styles>
                    <Style Selector="ToggleButton:pointerover /template/ ContentPresenter">
                        <Setter Property="Background" Value="{DynamicResource HoverGrip}"/>
                    </Style>
                    <Style Selector="ToggleButton:checked /template/ ContentPresenter">
                        <Setter Property="Background" Value="{DynamicResource AccentDangerDark}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource AccentDanger}"/>
                    </Style>
                </ToggleButton.Styles>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <PathIcon Data="{StaticResource IconSwords}" Width="14" Height="14" Foreground="{DynamicResource AccentDanger}"/>
                    <TextBlock Text="Tracker Walki" FontSize="13" Foreground="{DynamicResource TextSecondary}" FontWeight="SemiBold"/>
                </StackPanel>
            </ToggleButton>
        </StackPanel>

        <!-- OBSZAR PANELI -->
        <Grid Grid.Row="2" x:Name="ContentGrid" ColumnDefinitions="*, 16, *">
            
            <!-- KOLUMNA NOTATEK -->
            <Border x:Name="NotesPanel" Grid.Column="0" HorizontalAlignment="Stretch" Background="{DynamicResource SurfaceBackground}" CornerRadius="{StaticResource CardCornerRadius}" Padding="{StaticResource CardPadding}" BorderBrush="{DynamicResource BorderPrimary}" BorderThickness="1" MinWidth="600">
                <Grid RowDefinitions="Auto, *">
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,16">
                        <PathIcon Data="{StaticResource IconEdit}" Width="16" Height="16" Foreground="{DynamicResource AccentPrimary}"/>
                        <TextBlock Text="NOTATKI SESJI" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource AccentPrimary}" LetterSpacing="1"/>
                    </StackPanel>
                    
                    <sessions:SessionNotesView Grid.Row="1" />
                </Grid>
            </Border>

            <!-- SPLITTER PIONOWY -->
            <GridSplitter x:Name="PanelSplitter" Grid.Column="1" Background="Transparent" ResizeDirection="Columns" HorizontalAlignment="Stretch">
                <GridSplitter.Template>
                    <ControlTemplate>
                        <Border Background="Transparent" Width="16">
                            <Border Background="{DynamicResource HoverGrip}" Width="4" Height="40" CornerRadius="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>

            <!-- KOLUMNA WALKI -->
            <Border x:Name="CombatPanel" Grid.Column="2" HorizontalAlignment="Stretch" Background="{DynamicResource SurfaceBackground}" CornerRadius="{StaticResource CardCornerRadius}" Padding="{StaticResource CardPadding}" BorderBrush="{DynamicResource BorderPrimary}" BorderThickness="1" MinWidth="600">
                <Grid RowDefinitions="Auto, *">
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,16">
                        <PathIcon Data="{StaticResource IconSwords}" Width="16" Height="16" Foreground="{DynamicResource AccentDanger}"/>
                        <TextBlock Text="TRACKER WALKI" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource AccentDanger}" LetterSpacing="1"/>
                    </StackPanel>

                    <sessions:SessionCombatView Grid.Row="1" />
                </Grid>
            </Border>
            
        </Grid>

    </Grid>
</UserControl>