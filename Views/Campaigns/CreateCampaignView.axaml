<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DungeonApp.ViewModels"
             x:Class="DungeonApp.Views.CreateCampaignView"
             x:DataType="vm:CreateCampaignViewModel"
             Background="{DynamicResource AppBackground}"> <!-- Dajemy ciemne tło na cały ekran -->
    
    <UserControl.Styles>
        <Style Selector="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderSecondary}"/>
            <Setter Property="CornerRadius" Value="{DynamicResource InputBorderRadius}"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>
    </UserControl.Styles>

    <!-- Centrujemy zawartość na środku ekranu -->
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderSecondary}" 
                BorderThickness="1" 
                CornerRadius="{DynamicResource CardCornerRadius}" 
                Padding="{DynamicResource CardPadding}" 
                Width="500">
            
            <StackPanel Spacing="24">
                <!-- Tytuł Widoku -->
                <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                    <PathIcon Data="{StaticResource IconSwords}" Width="24" Height="24" Foreground="{DynamicResource AccentGold}"/>
                    <TextBlock Text="Stwórz Nową Kampanię" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Formularz -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Nazwa Kampanii" Foreground="{DynamicResource TextMuted}" FontSize="13" FontWeight="Medium"/>
                    <TextBox Text="{Binding Name}" PlaceholderText="np. Klątwa Strahda..."/>
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock Text="System RPG" Foreground="{DynamicResource TextMuted}" FontSize="13" FontWeight="Medium"/>
                    <TextBox Text="{Binding System}" PlaceholderText="np. D&amp;D 5e..."/>
                </StackPanel>

                <!-- Przyciski Akcji -->
                <Grid ColumnDefinitions="*, *" Margin="0,10,0,0">
                    <Button Grid.Column="0" Content="Anuluj" Classes="ghost"
                            Command="{Binding CancelCommand}"
                            BorderThickness="1" BorderBrush="{DynamicResource BorderSecondary}"
                            HorizontalAlignment="Stretch" Margin="0,0,6,0" />

                    <Button Grid.Column="1" Content="Zapisz" Classes="primary"
                            Command="{Binding CreateCommand}"
                            HorizontalAlignment="Stretch" Margin="6,0,0,0" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>