namespace DungeonApp.Desktop.Shell.Settings;

/// <summary>
/// The "Ustawienia" position's content - a frame-owned sidebar destination, available as soon as a
/// system is chosen, even with no campaign open. Deliberately empty: the author's own decision is
/// that this position exists and can be reached, not what it shows yet. It carries no state of its
/// own for exactly that reason - there is nothing yet to hold.
/// </summary>
public sealed class SettingsViewModel
{
}
